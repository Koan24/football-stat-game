# Data

This directory contains prototype football datasets and data import files.

The initial version of the project will use manually prepared JSON data.
External football APIs may later be used only for periodic data imports.

The application should use data stored in its own database instead of
querying external APIs during gameplay.